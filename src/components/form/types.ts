// 사주 폼 공유 타입
export interface SajuDateValue { year: number; month: number; day: number; }
export interface SajuTimeValue { hour: number; minute: number; }
export interface PillOption { value: string; label: string; disabled?: boolean; }
